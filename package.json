{
  "name": "task-management-board",
  "private": true,
  "description": "Task Management Board — Next.js, Node, Postgres, Tailwind CSS",
  "scripts": {
    "dev": "concurrently -n api,web -c green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "db:setup": "npm run db:setup --prefix backend",
    "db:reset": "npm run db:reset --prefix backend",
    "typecheck": "npm run typecheck --prefix backend && npm run typecheck --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
