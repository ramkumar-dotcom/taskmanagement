# Frontend (Next.js + Tailwind CSS + TypeScript)

This is only the website. The real setup instructions are in the
[root README](../README.md).

- Dev server: `npm run dev` (port 3000)
- API address: `NEXT_PUBLIC_API_URL` in `.env.local`
- Types: `@tmb/shared` (`shared/types.ts`)
